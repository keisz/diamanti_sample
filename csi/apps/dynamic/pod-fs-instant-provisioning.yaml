apiVersion: v1
kind: Pod
metadata:
  name: csi-pod-filesystem-instant-provisioning
spec:
  containers:
    - name: web-server
      image: nginx
      volumeMounts:
        - name: datavol-fs-instant-provisioning
          mountPath: /var/lib/www
  volumes:
    - name: datavol-fs-instant-provisioning
      persistentVolumeClaim:
        claimName: csi-pvc-filesystem-instant-provisioning
        readOnly: false

